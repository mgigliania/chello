# Optional. Leave this unset for the default setup, where each person enters
# their own Anthropic API key in Settings and pays for their own usage.
#
# Set it only if you want to pay for everyone who opens your link. Anyone with
# the URL can then spend your credit, so treat it as a private deployment.
ANTHROPIC_API_KEY=
